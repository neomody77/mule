"""
会话标题生成提示

用于根据首次对话内容自动生成有意义的会话标题
"""

SESSION_TITLE_PROMPT = """根据以下对话内容，生成一个简短的会话标题。

要求：
1. 标题应该简洁（3-8个字），准确概括会话主题
2. 使用与用户相同的语言（中文或英文）
3. 专注于用户的主要意图或任务
4. 避免使用通用词汇如"帮助"、"问题"

只输出标题文本，不要任何其他内容。

用户消息：
{user_message}

助手响应摘要：
{assistant_summary}
"""

TITLE_GENERATION_SYSTEM = """你是一个标题生成助手。你的任务是根据对话内容生成简洁、有意义的会话标题。

规则：
- 标题长度：3-8个字/词
- 语言：与用户输入保持一致
- 风格：简洁、具体、有描述性
- 避免：通用词汇、标点符号、emoji

示例：
- "添加用户认证" (好) vs "帮助解决问题" (差)
- "修复登录Bug" (好) vs "代码问题" (差)
- "重构API模块" (好) vs "代码改进" (差)
"""
